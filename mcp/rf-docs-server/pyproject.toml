[project]
name = "rf-docs-server"
version = "0.1.0"
description = "MCP server exposing Robot Framework keyword documentation (ARCHITECTURE.md §3.1)"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "robotframework>=7.0",
    "rapidfuzz>=3.0",
]

[tool.uv]
package = false
