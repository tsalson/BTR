Fix automatically by replacing not-allowed characters with allowed alternatives.
For dots: spell out abbreviations if short enough, otherwise just remove the
dot (e.g., 'Mr.' becomes 'Mr'). For other characters like '?', remove them
entirely. Continue without asking.
